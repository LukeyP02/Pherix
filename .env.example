# Copy to .env and fill in. .env is gitignored — never commit real keys.
# The dogfood agents (examples/dogfood/) read this; the library itself
# never calls an LLM and never reads any key.
ANTHROPIC_API_KEY=
